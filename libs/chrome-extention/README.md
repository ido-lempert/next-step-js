# chrome-extention

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build chrome-extention` to build the library.

## Running unit tests

Run `nx test chrome-extention` to execute the unit tests via [Jest](https://jestjs.io).
